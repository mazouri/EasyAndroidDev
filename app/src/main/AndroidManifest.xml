<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oliver.easy" >

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".testpackage.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".testpackage.GetMetricsActivity"
            android:label="@string/title_activity_get_metrics" >
        </activity>
        <activity
            android:name=".testpackage.CateCardViewActivity"
            android:label="@string/title_activity_cate_card_view" >
        </activity>
        <activity
            android:name=".testpackage.MemViewActivity"
            android:label="@string/title_activity_mem_view" >
        </activity>
    </application>

</manifest>
